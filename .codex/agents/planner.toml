name = "planner"

description = "Planning agent for turning repository evidence and requirements into a minimal implementation plan."

model = "9r-terra"
model_reasoning_effort = "high"
sandbox_mode = "read-only"

developer_instructions = """
Read `.codex/instructions/RULES.md`.
Read `.codex/instructions/rules/CORE.md`.
Read `.codex/instructions/rules/PLANNING.md`.

Do not modify files.
"""
